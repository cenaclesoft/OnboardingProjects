<Window
    x:Class="TodoApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TodoApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:TodoApp.ViewModel"
    Title="MainWindow"
    Width="800"
    Height="650"
    d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
    mc:Ignorable="d">

    <Window.Resources>
        <!--    -->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    Margin="20,0,0,0"
                    VerticalAlignment="Center"
                    FontFamily="Consolas"
                    FontSize="30"
                    FontWeight="Bold"
                    Text="Todo App" />

                <Grid Grid.Column="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="20" />
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" />
                        <TextBlock Grid.Column="1" Text="{Binding TodoList.CompletedCount}" />
                        <TextBlock Grid.Column="2" Text="/" />
                        <TextBlock Grid.Column="3" Text="{Binding TodoList.Count}" />
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <TextBlock Background="Aqua" />
            <Grid Margin="50,20,50,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  TODO: StackPanel로 NewTodoItem, TodoList 쌓기  -->
                <TextBlock Background="Azure" />

                <Border
                    Grid.Row="0"
                    Height="65"
                    Margin="0,0,0,10"
                    Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="65" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Grid.Column="0"
                            Margin="0,0,10,0"
                            Background="DarkKhaki">
                            <TextBox
                                Margin="10,0,10,0"
                                VerticalAlignment="Center"
                                FontSize="20"
                                Text="{Binding TodoInput, UpdateSourceTrigger=PropertyChanged}" />
                        </Border>

                        <Border Grid.Column="1" Background="DarkKhaki">
                            <Button Background="DarkOrange" Command="{Binding AddTodoCommand}" />
                        </Border>
                    </Grid>
                </Border>

                <Border
                    Grid.Row="1"
                    Margin="0,0,0,10"
                    Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding TodoList}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gray" BorderThickness="1">
                                        <Grid Height="65">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="15" />
                                                <ColumnDefinition Width="50" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="30" />
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Background="Coral" />

                                            <CheckBox
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                IsChecked="{Binding IsCompleted}" />

                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                Background="BlueViolet"
                                                FontSize="20"
                                                Text="{Binding TodoContent}" />

                                            <Button
                                                Grid.Column="3"
                                                Height="30"
                                                Background="DarkGoldenrod"
                                                Command="{Binding DataContext.DeleteTodoCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <TextBlock Background="Beige" />
        </Grid>

    </Grid>
</Window>
